Nel carrello non si elimina nulla ma si disattiva
C'è sia id.utente e id.sessione
Quando non si sa l'id.utente si utilizza l'id.sessione e quando l'utente si logga allora si sostituisce l'id.sessione con id.utente
Il file json verrà caricato da un file .php. Lo scopo è caricare i dati dinamicamente senza modificare la struttura del sito

Passaggi:
1. strutturare il database basandosi sui file json



Cosa ho fatto:
-fatto il database con i dati già inserito
-mancano molte immagini



Nuova consegna con i gruppi:
-quando un prodotto non ha un record completo lo si inserisce con un default
-l'utente deve compilare un questionario
-in base al questionario verrà fuori il profilo del singolo utente e un su possibile abbonamento (lo deve acquistare) dove gli vengono spediti i prodotti (non gli stessi per più volte)
